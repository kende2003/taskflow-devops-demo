name: Deploy Auth Service

on:
  push:
    branches: [ "main" ]
    paths:
      - "backend/auth/**" 

jobs:
  deploy-auth:
    runs-on: ubuntu-latest

    steps:
      # ---- CHECKOUT ----
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---- AWS LOGIN ----
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-central-1

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build docker image
        run: |
          cd backend/auth
          docker build -t auth-api:${{ github.sha }} .

      - name: Push Docker image to ECR
        run: |
          ECR_REPO="${{ secrets.ECR_REPO_URL }}"   # pl: xxx.dkr.ecr.eu-central-1.amazonaws.com/auth-api
          docker tag auth-api:${{ github.sha }} $ECR_REPO:${{ github.sha }}
          docker push $ECR_REPO:${{ github.sha }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Terraform init
        run: |
          cd terraform/infra
          terraform init

      - name: Terraform validate
        run: |
          cd terraform/infra
          terraform validate

      - name: Terraform apply (auto approve)
        run: |
          cd terraform/infra
          terraform apply -auto-approve \
            -var "auth_api_image_tag=${{ github.sha }}" \
            -var "auth_db_password=${{ secrets.AUTH_DB_PASSWORD }}" \
            -var "aws_iam_user=${{ secrets.AWS_IAM_USER }}"
